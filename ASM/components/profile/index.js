import React from "react";
import { styles } from "./styles";
import { View, Text, Image, FlatList, TextInput, TouchableOpacity, ScrollView } from "react-native";



function Profile() {
  return (
  <ScrollView>
    <Image source={require('../../assets/rv_logo.png')}/>
  </ScrollView>  
    
  );
}
export default Profile;

