import React from "react";
import { View,Text } from "react-native";

const SingerScreen = () =>{
    return(
         <View style={{ flex: 1, justifyContent: "center", alignItems: "center", backgroundColor: "#f4cccc" }}>
                    <Text style={{ fontSize: 24, fontWeight: "bold", color: "#FF6C22" }}>
                Singer 
            </Text>
        </View>
    )
}

export default SingerScreen;
